apiVersion: apps/v1
kind: Deployment
metadata:
  name: advance-scoring
  namespace: advance-system
  labels:
    app: advance-scoring
spec:
  replicas: 1
  selector:
    matchLabels:
      app: advance-scoring
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: advance-scoring
    spec:
      containers:
        - name: advance-scoring
          image: ghcr.io/finuniversity/advance-scoring:latest
          ports:
            - containerPort: 8084
          envFrom:
            - configMapRef:
                name: advance-config
            - secretRef:
                name: advance-secrets
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8084
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8084
            initialDelaySeconds: 60
            periodSeconds: 30
          lifecycle:
            preStop:
              exec:
                command: ["/bin/sh", "-c", "sleep 10"]
---
apiVersion: v1
kind: Service
metadata:
  name: advance-scoring
  namespace: advance-system
spec:
  selector:
    app: advance-scoring
  ports:
    - port: 8084
      targetPort: 8084
