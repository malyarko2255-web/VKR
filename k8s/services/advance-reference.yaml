apiVersion: apps/v1
kind: Deployment
metadata:
  name: advance-reference
  namespace: advance-system
  labels:
    app: advance-reference
spec:
  replicas: 1
  selector:
    matchLabels:
      app: advance-reference
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: advance-reference
    spec:
      containers:
        - name: advance-reference
          image: ghcr.io/finuniversity/advance-reference:latest
          ports:
            - containerPort: 8085
          envFrom:
            - configMapRef:
                name: advance-config
            - secretRef:
                name: advance-secrets
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8085
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8085
            initialDelaySeconds: 60
            periodSeconds: 30
          lifecycle:
            preStop:
              exec:
                command: ["/bin/sh", "-c", "sleep 10"]
---
apiVersion: v1
kind: Service
metadata:
  name: advance-reference
  namespace: advance-system
spec:
  selector:
    app: advance-reference
  ports:
    - port: 8085
      targetPort: 8085
