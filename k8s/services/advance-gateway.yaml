apiVersion: apps/v1
kind: Deployment
metadata:
  name: advance-gateway
  namespace: advance-system
  labels:
    app: advance-gateway
spec:
  replicas: 2
  selector:
    matchLabels:
      app: advance-gateway
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: advance-gateway
    spec:
      containers:
        - name: advance-gateway
          image: ghcr.io/finuniversity/advance-gateway:latest
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: advance-config
            - secretRef:
                name: advance-secrets
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 20
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 40
            periodSeconds: 30
          lifecycle:
            preStop:
              exec:
                command: ["/bin/sh", "-c", "sleep 10"]
---
apiVersion: v1
kind: Service
metadata:
  name: advance-gateway
  namespace: advance-system
spec:
  selector:
    app: advance-gateway
  ports:
    - port: 8080
      targetPort: 8080
